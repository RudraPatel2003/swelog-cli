---
title: Linear
description: Record the Linear issues assigned to you, grouped by status.
---

Record the Linear issues assigned to you into your work file.

## Setup

Set the exact Linear assignee name in `swelog.json`. See [Configuration](/getting-started/configuration/) for more information.

```json
{
  "linearUsername": "rudrapatel"
}
```

## Usage

Run the fetch command:

```sh
swelog fetch linear
```

On first use, swelog prints a Linear authorization URL and attempts to open
it in your browser. If the browser does not open, use the printed URL.

## Backfilling

To backfill a previous date, provide `MM-DD-YYYY`:

```sh
swelog fetch linear --date 08-17-2026
```

This records the issues that Linear shows activity on for that date, including the
ones you completed or canceled

`--yesterday` does the same for the day before:

```sh
swelog fetch linear --yesterday
```

For last Friday, use `--last-friday` instead of working out the date:

```sh
swelog fetch linear --last-friday
```

## Output

The generated section groups issues by status:

```markdown
## Linear

### In Progress

- [ENG-123](https://linear.app/...) Implement OAuth callback handling

### Todo

- [ENG-456](https://linear.app/...) Document the Linear integration
```

## Limitations of backfilling

The Linear MCP server does not expose issue history, so swelog can only match on
the timestamps Linear stores on the issue itself, which are `updatedAt`, `completedAt`,
`canceledAt`, `startedAt`, and `createdAt`. If the timestamp matches the date you pass in, it is recorded.

`completedAt`, `canceledAt`, `startedAt`, and `createdAt` never change, so
issues you started, finished, canceled, or filed on a date come back no matter
how long you wait to backfill.

The `--date` flag will not fetch archived issues.

## Switching accounts or organizations

Update `linearUsername` when needed, clear the stored authorization, and fetch
again:

```sh
swelog auth clear linear
swelog fetch linear
```

The next fetch starts a new browser authorization flow.
