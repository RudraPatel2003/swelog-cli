---
title: GitHub
description: Record the pull requests you opened and merged into your work file.
---

Record the pull requests you opened and merged into your work file.

## Usage

```sh
swelog fetch github
```

The first run asks for a
[GitHub personal access token](https://github.com/settings/tokens) with the
`repo` scope and stores it in your operating system keyring.

## Backfilling

To fetch a previous date, provide `MM-DD-YYYY`:

```sh
swelog fetch github --date 08-17-2026
```

For the previous day, use `--yesterday` instead of working out the date:

```sh
swelog fetch github --yesterday
```

For last Friday, use `--last-friday` instead of working out the date:

```sh
swelog fetch github --last-friday
```

## Output

The command creates or updates the `## GitHub` section before `## Log`.

```markdown
## GitHub

### Opened

- "Test PR" ([#123](https://github.com/swelog-cli/swelog-cli/pull/123)) in [swelog-cli/swelog-cli](https://github.com/swelog-cli/swelog-cli)

### Merged

- "Test PR 2" ([#456](https://github.com/swelog-cli/swelog-cli/pull/456)) in [swelog-cli/swelog-cli](https://github.com/swelog-cli/swelog-cli)
```
